[pytest]
testpaths = tests
pythonpath = .

env =
    PLANNER_PREWARM=0
    LLM_PRIORITY=local_only
    USE_CLOUD_LLM=0
    TESTING=1

filterwarnings =
    ignore:datetime.datetime.utcnow.*:DeprecationWarning